defaultCiPipeline {
	operatingSystem = ['ubuntu']
	instanceSize = 'medium'

	ciBuildDockerfile = 'python.Dockerfile'

	packageId = 'bridge'

	publisher = 'docker'
	dockerImageName = 'symbolplatform/bridge'

	codeCoverageTool = 'coverage'
	minimumCodeCoverage = 95
}
