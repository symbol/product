// Variant-specific page-section components, keyed by the name referenced from page config
// (see config/pages/*.json `additionalSections[].component`). The nem variant contributes no
// additional sections.
const components = {};

export default components;
