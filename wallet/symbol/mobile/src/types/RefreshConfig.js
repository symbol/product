/**
 * Configuration for the pull-to-refresh indicator in a scrollable list.
 * @typedef {object} RefreshConfig
 * @property {boolean} isRefreshing - Indicates if the refresh is in progress.
 * @property {function(): void} onRefresh - Function to call when a refresh is triggered.
 * @property {string} color - Color of the refresh indicator.
 */

export {};
