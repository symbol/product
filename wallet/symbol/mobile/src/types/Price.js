/**
 * Current market price with its currency denomination.
 * @typedef {object} Price
 * @property {number} value - The current price value.
 * @property {string} currency - The currency ticker.
 */

export {};
